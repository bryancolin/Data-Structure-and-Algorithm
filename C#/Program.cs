using System;

namespace C_
{
    class Program
    {
        static void Main(string[] args)
        {
            // Dotnet d = new Dotnet();
            // Console.WriteLine(d.start());
            // Console.WriteLine(d.install());
            // Console.WriteLine(d.compile());

            // d.Title = "dotnet";
            // Console.WriteLine(d.Title);

            // Console.WriteLine("Hi, Let me..");
            // int userInput = Convert.ToInt32(Console.ReadLine());
            // Console.WriteLine("Number {0}",userInput);

            // Console.ReadKey();
            Test t = new Test();
            t.Print();
        }
    }
}
